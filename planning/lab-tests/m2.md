# Lab-test evidence — M2: Speak the Universal Contract

**Machine:** Apple Silicon (arm64), 16 GB RAM  
**Date:** 2026-07-05  
**Runtime:** Rancher Desktop — docker client `29.5.3-rd`, server `29.5.2` (dockerd/moby)  
**Model server:** Ollama `0.17.4` (native, Metal), serving on `:11434`  
**Model:** `qwen2.5:1.5b` (986 MB, Q4_K_M)  
**Validated by:** automated Bash tool calls with `PATH="$HOME/.rd/bin:$PATH"` prefix

> Note: in lab.md, all commands use plain `docker`/`docker compose` (learners have it on PATH).
> In this validation session, every docker command was prefixed with `PATH="$HOME/.rd/bin:$PATH"`.

---

## Sanity — `docker version`

**Command:**
```bash
PATH="$HOME/.rd/bin:$PATH" docker version
```

**Real output:**
```
Client:
 Version:           29.5.3-rd
 API version:       1.54
 Go version:        go1.26.4
 Git commit:        5d9ffe3
 Built:             Thu Jun  4 07:33:36 2026
 OS/Arch:           darwin/arm64
 Context:           rancher-desktop

Server:
 Engine:
  Version:          29.5.2
  API version:      1.54 (minimum version 1.41)
  Go version:       go1.25.10
  Git commit:       568f755ebeb1ac9c6a8febbda6cd371ea0a9630b
  Built:            Sun May 31 13:53:18 2026
  OS/Arch:          linux/arm64
  Experimental:     false
 containerd:
  Version:          v2.3.2
  GitCommit:        fff62f14765df376e5fc36f5a8f8e795b5670f61
 runc:
  Version:          1.4.0
  GitCommit:        8bd78a9977e604c4d5f67a7415d7b8b8c109cdc4
 docker-init:
  Version:          0.19.0
```

**Status:** ✅ PASS

---

## Lab Step 1a — `GET /v1/models` from host

**Command:**
```bash
curl -s http://localhost:11434/v1/models | python3 -m json.tool
```

**Real output:**
```json
{
    "object": "list",
    "data": [
        {
            "id": "qwen2.5:1.5b",
            "object": "model",
            "created": 1783237329,
            "owned_by": "library"
        }
    ]
}
```

**Status:** ✅ PASS

**Deviation from lab.md:** `created` was `1783237329` (a real epoch timestamp), not the placeholder `1720000000`. Structure is correct. Updated in lab.md.

---

## Lab Step 1b — Chat completions from host

**Command:**
```bash
curl -s http://localhost:11434/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "qwen2.5:1.5b",
    "messages": [{"role": "user", "content": "Explain containers in one sentence."}]
  }' | python3 -m json.tool
```

**Real output:**
```json
{
    "id": "chatcmpl-184",
    "object": "chat.completion",
    "created": 1783249846,
    "model": "qwen2.5:1.5b",
    "system_fingerprint": "fp_ollama",
    "choices": [
        {
            "index": 0,
            "message": {
                "role": "assistant",
                "content": "Containers are lightweight, portable executable packages that include all necessary components (code and dependencies) to run software independently."
            },
            "finish_reason": "stop"
        }
    ],
    "usage": {
        "prompt_tokens": 36,
        "completion_tokens": 23,
        "total_tokens": 59
    }
}
```

**Status:** ✅ PASS

**Deviations from lab.md:**
- `id` is `"chatcmpl-184"` (numeric counter suffix), not `"chatcmpl-abc123"`
- `created` is a real epoch timestamp, not `1720000001`
- **New field:** `"system_fingerprint": "fp_ollama"` — Ollama includes this field; lab expected output omitted it entirely
- Token counts: `prompt_tokens: 36`, `completion_tokens: 23`, `total_tokens: 59` (not 15/28/43)
- Content varies (generative); captured sentence noted in lab.md
- All of the above updated in lab.md

---

## Lab Step 1c — `GET /v1/models` from inside container

**Command:**
```bash
PATH="$HOME/.rd/bin:$PATH" docker run --rm curlimages/curl:latest -s \
  http://host.docker.internal:11434/v1/models
```

**Real output:**
```json
{"object":"list","data":[{"id":"qwen2.5:1.5b","object":"model","created":1783237329,"owned_by":"library"}]}
```

**Status:** ✅ PASS

**Deviation from lab.md:** `created` was `1783237329`, not placeholder `1720000000`. Updated in lab.md.

---

## Lab Step 1c — Chat completions from inside container

**Command:**
```bash
PATH="$HOME/.rd/bin:$PATH" docker run --rm curlimages/curl:latest -s \
  http://host.docker.internal:11434/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"qwen2.5:1.5b","messages":[{"role":"user","content":"Say hi in 5 words."}]}'
```

**Real output:**
```json
{"id":"chatcmpl-460","object":"chat.completion","created":1783249856,"model":"qwen2.5:1.5b","system_fingerprint":"fp_ollama","choices":[{"index":0,"message":{"role":"assistant","content":"Hello there! Ready to chat now."},"finish_reason":"stop"}],"usage":{"prompt_tokens":36,"completion_tokens":9,"total_tokens":45}}
```

**Status:** ✅ PASS

**Deviations from lab.md:**
- `id` is `"chatcmpl-460"`, not `"chatcmpl-def456"`
- `created` is a real epoch timestamp, not `1720000002`
- **New field:** `"system_fingerprint": "fp_ollama"` — missing from lab expected output
- Token counts: `prompt_tokens: 36`, `completion_tokens: 9`, `total_tokens: 45` (not 12/6/18)
- Content varies (generative)
- All updated in lab.md

---

## Lab Step 3 — `docker build -t m2-client labs/m2/`

**Command:**
```bash
PATH="$HOME/.rd/bin:$PATH" docker build -t m2-client /Users/bean/work/courses/containerai/course/labs/m2/
```

**Real output (condensed — pip install log truncated):**
```
#0 building with "rancher-desktop" instance using docker driver

#1 [internal] load build definition from Dockerfile
#1 DONE 0.0s

#2 [internal] load metadata for docker.io/library/python:3.12-slim
#2 DONE 2.7s

#3 [internal] load .dockerignore
#3 DONE 0.0s

#4 [internal] load build context
#4 DONE 0.0s

#5 [1/4] FROM docker.io/library/python:3.12-slim@sha256:423ed6ab25b1921a477529254bfeeabf5855151dc2c3141699a1bfc852199fbf
#5 DONE 10.3s

#6 [2/4] RUN pip install --no-cache-dir openai
#6 0.780 Collecting openai
#6 0.851   Downloading openai-2.44.0-py3-none-any.whl.metadata (34 kB)
#6 ...
#6 3.757 Successfully installed annotated-types-0.7.0 anyio-4.14.1 certifi-2026.6.17 distro-1.9.0 h11-0.16.0 httpcore-1.0.9 httpx-0.28.1 idna-3.18 jiter-0.16.0 openai-2.44.0 pydantic-2.13.4 pydantic-core-2.46.4 sniffio-1.3.1 tqdm-4.68.3 typing-extensions-4.16.0 typing-inspection-0.4.2
#6 DONE 4.0s

#7 [3/4] WORKDIR /app
#7 DONE 0.0s

#8 [4/4] COPY client.py .
#8 DONE 0.0s

#9 exporting to image
#9 exporting layers 0.6s done
#9 exporting manifest sha256:c729da652048bfa53da3ae29ea9be7b34f0da91994c36216e2128ecf3b70820e done
#9 naming to docker.io/library/m2-client:latest done
#9 DONE 0.9s
```

**Status:** ✅ PASS

**Deviations from lab.md:**
- Real output uses BuildKit `#N` step notation, not the simplified `[+] Building XX.Xs` format shown in lab
- Build has **4 steps** (1/4 through 4/4): FROM, RUN pip install, WORKDIR /app, COPY — lab expected output showed only 3 steps (`[1/3]`, `[2/3]`, `[3/3]`) because WORKDIR was not listed as a separate step
- `openai` package version installed: `2.44.0`
- Image SHA: `sha256:c729da652048bfa53da3ae29ea9be7b34f0da91994c36216e2128ecf3b70820e`
- Build took ~15 seconds total (fresh pull + pip install on good connection); lab said `45.2s`
- All updated in lab.md

---

## Lab Step 3 — `docker run --rm m2-client`

**Command:**
```bash
PATH="$HOME/.rd/bin:$PATH" docker run --rm m2-client
```

**Real output:**
```
Containers are lightweight, portable executable packages that include everything needed to run software independently.
```

**Status:** ✅ PASS — model returns a sentence, not an error. Content varies (generative).

**Deviation from lab.md:** Content varies; real captured sentence differs from placeholder. Updated in lab.md (noted as representative).

---

## Lab Step 4 — `docker compose -f labs/m2/compose.yaml run --rm client`

**Command (tested from repo root — both relative and absolute path forms work):**
```bash
PATH="$HOME/.rd/bin:$PATH" docker compose -f labs/m2/compose.yaml run --rm client
```

**Real output:**
```
 Network m2_default Creating
 Network m2_default Created
 Container m2-client-run-81c3d51533d9 Creating
 Container m2-client-run-81c3d51533d9 Created
Containers are lightweight virtualizations that allow applications to run isolated from each other on a single machine.
```

> Note: network/container creation lines only appear on first run. Subsequent runs skip them (network already exists). The last line is the model's reply.

**Status:** ✅ PASS

**Deviations from lab.md:**
- The `docker compose -f labs/m2/compose.yaml run --rm client` command form **works from repo root** — Docker Compose resolves `build: .` relative to the compose file's directory, not the CWD. No command fix needed.
- On first run, two network/container lines are printed before the model reply. These are not in the lab's expected output.
- Content varies (generative); real captured sentence differs from placeholder. Updated in lab.md.

---

## Summary

| Step | Command | Result | Notes |
|------|---------|--------|-------|
| 1a | `curl .../v1/models` (host) | ✅ PASS | Correct shape; `created` is a real timestamp |
| 1b | `curl .../v1/chat/completions` (host) | ✅ PASS | `system_fingerprint` field added; token counts corrected |
| 1c | `docker run curlimages/curl .../v1/models` | ✅ PASS | host.docker.internal works; `created` real timestamp |
| 1c | `docker run curlimages/curl .../v1/chat/completions` | ✅ PASS | `system_fingerprint` field added; token counts corrected |
| 3 build | `docker build -t m2-client labs/m2/` | ✅ PASS | BuildKit format; 4 steps not 3; openai 2.44.0 |
| 3 run | `docker run --rm m2-client` | ✅ PASS | Sentence returned; content varies |
| 4 | `docker compose -f labs/m2/compose.yaml run --rm client` | ✅ PASS | Works from repo root; sentence returned |

**All lab commands work as written. Key corrections to lab.md:**
1. `system_fingerprint: "fp_ollama"` added to both chat completion expected outputs (Step 1b and 1c).
2. Token counts corrected in Step 1b (36/23/59) and Step 1c (36/9/45).
3. `id` and `created` fields updated to real-shaped values.
4. Build expected output rewritten to match BuildKit `#N` format with 4 steps (was 3).
5. Generative reply text updated for Steps 3 and 4.
