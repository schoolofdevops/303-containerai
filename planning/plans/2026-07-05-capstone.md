# Capstone · Ship the Acme AI Support Platform — Plan

> Controller validated `labs/capstone/platform-check.sh` live (all layers green). Subagent authors prose.
> Integration module — composes M1–M8; less new material, more "put it together + prove it ships."

**Goal:** Guided end-to-end integration: **serve → RAG → agent → crew → package → secure → ship**, then
a **portability proof** (swap the runtime, re-run unchanged). Runs on any OCI runtime, 16 GB laptop.

**Validated live (`lab-tests/capstone.md`):** `labs/capstone/platform-check.sh` verifies the whole stack
(runtime, native Ollama + `qwen2.5:1.5b` + `nomic-embed-text`, `host.docker.internal` wiring, `kit`,
`thv`, `syft`/`trivy`/`grype`/`cosign`) → "PLATFORM READY". Each of the 8 steps is validated in its
module's `lab-tests/*.md`.

## Files (controller created labs/capstone/*)
- `labs/capstone/platform-check.sh`, `labs/capstone/README.md`
- Create: `site/docs/capstone/index.md` (single integration page — or lesson + lab if cleaner)
- Modify: `site/sidebars.ts` (add Capstone after M8)

## Task 1 — Capstone doc (`site/docs/capstone/`)
A guided integration walkthrough (analogy + ≥1 Mermaid of the full platform). Sections:
1. **The platform at a glance** — Mermaid: native model ← {Docs Assistant, Support Agent, Incident Crew}
   → MCP tools (ToolHive) + vector memory (Chroma); packaged as ModelKit; secured (SBOM/scan/sign);
   shipped via CI. Show the container boundary (model native, everything else containerized).
2. **Run it end-to-end** — copy-runnable: `./platform-check.sh` (readiness), then serve (M2/M3), run the
   crew (M7 `docker compose run --rm crew "..."`) using the Docs Assistant knowledge, package the model
   (M4 `kit pack`/`push`), secure the crew image (M8 `./secure-image.sh`), and the CI pipeline (M8
   workflow). Each step links to its module. Use the real `platform-check` output from `lab-tests/capstone.md`.
3. **Portability proof** — the same commands on Colima ↔ Rancher ↔ OrbStack ↔ Podman; nothing changes
   because it's OCI + the Compose Spec + a native model behind `host.docker.internal`.
4. **What you built + where to go next** — recap the ladder (naive RAG → agentic RAG → agent → crew),
   the optional take-home second use case, M3B (LoRA/QLoRA) as an extension.
~1000–1400 words. Include a **quiz** (`quiz.mdx`, 5 Qs, EXACT `<Quiz>` schema from CLAUDE.md) covering the
end-to-end architecture + the portability principle.

## Task 2 — Sidebar + build gate
Add a Capstone category (index/lesson + quiz). Build green. Admonitions bracket form. Quiz schema correct
(`grep -c 'prompt:' == 5`). Commit per task. Controller review → push → verify deploy → Capstone pages 200.
