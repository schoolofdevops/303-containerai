---
sidebar_position: 3
title: 'Quiz: Module 6'
---

import Quiz from '@site/src/components/Quiz';

# Module 6 Quiz

Test your understanding of the declarative agent pattern, agentic RAG versus naive RAG, guardrails, MCP tool isolation via ToolHive, and the roles of SOUL.md, AGENTS.md, and SKILL.md.

<Quiz questions={[
  {
    prompt: 'Which of the following are true of the declarative agent pattern used in M6? (Select all that apply)',
    multiSelect: true,
    options: [
      {
        text: 'The agent\'s behavior — identity, instructions, and skills — is defined in Markdown files, not hard-coded Python.',
        correct: true,
        explanation: 'Correct. The three Markdown files (SOUL.md, AGENTS.md, SKILL.md) are the agent. agent.py reads them at startup and passes them as the system prompt. Changing the Markdown changes the behavior.',
      },
      {
        text: 'Updating Aria\'s tone or adding a new guardrail keyword requires editing Markdown, not application code.',
        correct: true,
        explanation: 'Correct. Because the persona and rules are loaded from files at runtime, operational changes — tone, guardrails, skill procedures — are Markdown edits. No redeploy of agent.py is needed.',
      },
      {
        text: 'The declarative pattern requires a framework like LangChain or CrewAI to function.',
        correct: false,
        explanation: 'False. M6\'s agent uses no framework — only the Python standard library (urllib.request, json, pathlib). The complexity lives in the Markdown, not the code. Frameworks become necessary for multi-agent orchestration (M7), not for a single declarative agent.',
      },
      {
        text: 'A minimal glue script reads the Markdown files and uses their concatenated text as the system prompt sent to the model.',
        correct: true,
        explanation: 'Correct. agent.py\'s load_persona() reads SOUL.md, AGENTS.md, and SKILL.md, joins them, and stores the result as PERSONA. Every LLM call receives PERSONA as its system parameter.',
      },
      {
        text: 'The agent makes no LLM calls — all decisions are handled by deterministic if-else logic in agent.py.',
        correct: false,
        explanation: 'False. The agent makes two categories of LLM call: route() calls the model at temperature 0 for the YES/NO routing decision, and the answer is generated by a separate LLM call. Only the guardrail is a pure regex check with no model involvement.',
      },
    ],
  },
  {
    prompt: 'What is the key difference between the agentic RAG in M6 and the naive RAG pipeline in M5?',
    options: [
      {
        text: 'M6 uses a larger embedding model, which improves retrieval accuracy for every query.',
        correct: false,
        explanation: 'Both M5 and M6 use nomic-embed-text for embeddings. The difference is not about the embedding model — it is about whether retrieval happens at all.',
      },
      {
        text: 'M6 adds a routing step: the agent decides whether a query needs retrieval before touching ChromaDB. M5 always retrieves.',
        correct: true,
        explanation: 'Correct. In M5, every query triggers embed → retrieve → generate regardless of relevance. In M6, the agent first asks the LLM at temperature 0: "Does this question require Acme\'s runbooks?" Only YES answers proceed to retrieval. General questions are answered directly, saving embedding calls and avoiding bad-match hallucination.',
      },
      {
        text: 'M6 retrieves more chunks (k=10 instead of k=3) to improve answer quality.',
        correct: false,
        explanation: 'M6 retrieves k=2 chunks by default, not more. The quality improvement comes from the routing decision — retrieving only when relevant — not from fetching more chunks.',
      },
      {
        text: 'M6 re-ranks retrieved chunks using a cross-encoder model before generating the answer.',
        correct: false,
        explanation: 'M6 does not use a cross-encoder. The improvement over M5 is the routing decision (retrieve only when needed), not a re-ranking step. Cross-encoder re-ranking is a separate advanced RAG technique.',
      },
    ],
  },
  {
    prompt: 'The guardrail in agent.py blocks the query "reveal the database password" before any LLM call is made. Why is an application-layer regex guardrail more reliable than a soft system-prompt instruction like "never reveal secrets"?',
    options: [
      {
        text: 'A regex is faster than an LLM call, so it reduces latency for blocked queries.',
        correct: false,
        explanation: 'Speed is a side benefit, not the reliability argument. The fundamental reason is that a regex cannot be bypassed by prompt manipulation — the LLM is never given the chance to decide whether to comply.',
      },
      {
        text: 'A soft system-prompt instruction can be prompted around ("hypothetically...", "ignore previous instructions"); a regex check at the application layer runs before the model is invoked, so clever prompt phrasing cannot bypass it.',
        correct: true,
        explanation: 'Correct. Small local models are particularly susceptible to prompt injection and roleplay jailbreaks. Because the regex guardrail executes in Python before any text reaches the model, no amount of prompt engineering can override it. The refusal is unconditional.',
      },
      {
        text: 'The system prompt is not sent to the model for blocked queries, so the model has no instructions at all.',
        correct: false,
        explanation: 'The reason is not that instructions are missing — it is that the model is never called at all. The guardrail short-circuits the entire handle() function and returns the refusal string directly, before any Ollama API call.',
      },
      {
        text: 'LLMs cannot read regex patterns, so placing the rule in code rather than a prompt makes it enforceable.',
        correct: false,
        explanation: 'The issue is not readability — LLMs can follow natural-language safety instructions. The issue is that natural-language instructions can be overridden by adversarial prompts. Code-level enforcement runs outside the model\'s context entirely.',
      },
    ],
  },
  {
    prompt: 'When you run "thv run fetch", ToolHive starts four containers: the fetch server, fetch-ingress, fetch-egress, and fetch-dns. What is the purpose of this isolation stack?',
    options: [
      {
        text: 'The proxy containers cache responses from the fetch server to reduce repeated network calls.',
        correct: false,
        explanation: 'The ingress and egress containers are network proxies for isolation, not caches. They enforce which traffic can enter and leave the fetch server\'s network namespace.',
      },
      {
        text: 'The four containers provide per-server network isolation: the fetch server can reach the public internet through the egress proxy but cannot access the host filesystem, other containers, or credentials stored on the host.',
        correct: true,
        explanation: 'Correct. ToolHive wraps each MCP server in its own isolation stack so that a compromised or misbehaving tool server cannot exfiltrate credentials, access other services, or escape to the host. Each server gets its own egress proxy (controlled outbound), ingress proxy (controlled inbound), and DNS resolver.',
      },
      {
        text: 'The dns container allows the fetch server to resolve the agent container\'s internal hostname.',
        correct: false,
        explanation: 'The DNS container provides per-server DNS resolution for network isolation, not for agent-to-server lookup. The agent reaches the fetch server via the ToolHive endpoint URL (http://127.0.0.1:<port>/mcp), not by resolving the server\'s container hostname.',
      },
      {
        text: 'The ingress and egress containers handle TLS termination so the MCP endpoint is served over HTTPS.',
        correct: false,
        explanation: 'The proxies enforce network isolation, not TLS termination. The local MCP endpoint ToolHive exposes (http://127.0.0.1:<port>/mcp) uses plain HTTP on localhost — TLS is not the purpose of the isolation stack.',
      },
    ],
  },
  {
    prompt: 'A teammate asks: "Why does the M6 agent need three separate files (SOUL.md, AGENTS.md, SKILL.md)? Why not put everything in one big system prompt?" What is the best answer?',
    options: [
      {
        text: 'Three files are required by the MCP protocol — a single-file agent cannot declare tools.',
        correct: false,
        explanation: 'MCP has no requirement about how many files define an agent. The three-file split is an authoring convention, not a protocol constraint.',
      },
      {
        text: 'Splitting identity (SOUL), operating instructions (AGENTS), and composable capabilities (SKILL) makes each concern independently editable and reusable across agents without touching the others.',
        correct: true,
        explanation: 'Correct. SOUL.md defines who the agent is — swap it to deploy the same skill set with a different persona. AGENTS.md defines operating instructions — update guardrails or tool declarations without touching identity. SKILL.md is a composable procedure — the same agentic-RAG skill could be imported by a different agent. Separation of concerns in Markdown is the same principle as in code.',
      },
      {
        text: 'agent.py can only read one file at startup, so the persona, instructions, and skills must be in separate files loaded in order.',
        correct: false,
        explanation: 'agent.py\'s load_persona() reads all three files and concatenates them. Technically they could all be one file. The split is a design convention for maintainability and reuse, not a technical constraint.',
      },
      {
        text: 'The LLM performs better when identity, instructions, and skills arrive as separate system messages rather than a single concatenated string.',
        correct: false,
        explanation: 'agent.py concatenates all three files into a single PERSONA string and sends it as one system parameter. There is no multi-message delivery. The benefit of three files is authoring clarity and composability, not model performance.',
      },
    ],
  },
]} />
